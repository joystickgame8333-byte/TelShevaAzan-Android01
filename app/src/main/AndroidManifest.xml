<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".PrayerNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.telshevaazan.ACTION_NOTIFY" />
                <action android:name="com.example.telshevaazan.ACTION_RESCHEDULE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".WidgetUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.telshevaazan.ACTION_UPDATE_WIDGETS" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NextPrayerWidgetProvider"
            android:exported="false"
            android:label="الصلاة القادمة">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next_prayer" />
        </receiver>

        <receiver
            android:name=".FajrCircleWidgetProvider"
            android:exported="false"
            android:label="الفجر والتوقيت">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle" />
        </receiver>

        <receiver
            android:name=".NextCountdownCircleWidgetProvider"
            android:exported="false"
            android:label="متبقي للصلاة">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle" />
        </receiver>

        <receiver
            android:name=".IqamaMinutesCircleWidgetProvider"
            android:exported="false"
            android:label="مدة الإقامة">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle" />
        </receiver>

        <receiver
            android:name=".IqamaTimeCircleWidgetProvider"
            android:exported="false"
            android:label="وقت الإقامة">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle" />
        </receiver>

        <receiver
            android:name=".SunriseCircleWidgetProvider"
            android:exported="false"
            android:label="وقت الشروق">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle" />
        </receiver>
    </application>
</manifest>
